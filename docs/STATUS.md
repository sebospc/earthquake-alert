# Current status

Updated: 2026-09-25 23:20 UTC. Whoever finishes a piece of work updates this file.

## What is running

| where | what | how to see it |
|---|---|---|
| AWS EC2 `i-0d1c0b6dd02e0ae61`, sa-east-1, spot r8i.large | receptors `chaparral` (emulator-5554), `quibdo` (emulator-5556) and canary `general-santos` (emulator-5558, since 25-sep 12:00Z), gateway, sensor-health, watchdog | `ssh -i ~/.ssh/aea-lab.pem ubuntu@56.126.124.1` (Elastic IP since 2026-09-25 ~00:18Z, survives spot stop/start); `curl -s 127.0.0.1:8787/status`. Public HTTPS (staging): https://d1o3i68tksfjqi.cloudfront.net → Caddy :80 → gateway |
| Mac | control fleet RETIRED 2026-09-24 19:15 UTC: launchd job removed (plist kept as `aea-lab/com.earthquakes.aea-lab.plist.retired`), 4 emulators shut down. AVDs (with the personal Google account) still on disk | - |
| Mac | certifier `monitor/monitor.py`, started by hand (not a service) | `docs/qa/cert/AAAA-MM-DD.md` |
| AWS | CloudWatch (since 25-sep 00:30): logs + 5 alarms to SNS email (gateway down, receptor uncovered 10 min, AEA location > 21 h, AEA_NOT_OK, NUDGE_FAILED). Stacks aea-lab-cloudwatch, aea-lab-alarms-i-0d1c0b6dd02e0ae61 | CloudWatch console, sa-east-1 |

adb on the EC2: always `sudo -u aea -H /opt/android-sdk/platform-tools/adb`.
Deploy: gateway and host tools go through the pipeline (push to main → CI → approve `production`; live since ff276ae, 25-sep). A bootstrap rerun, only for host changes: `git archive origin/main` to the host + `sudo EMULATOR_COUNT=3 VAPID_SUBJECT=mailto:alertas@example.com ./aws-bootstrap.sh`; APK: `./aws-bootstrap.sh listener <apk> emulator-NNNN=<sensor_id>...` (explicit pairs).
Live sha: `8387d62` (gateway batch + listener fix), deployed 25-sep 23:11 UTC, verified on host: chaparral/quibdo/general-santos all `covered: true`, `aea_ok: true`, fresh heartbeats. `719b15c` (backup batch) and `f407540` (iOS app) pushed and CI green, not yet deployed — backup's timer install still needs the stack update + passphrase (next steps below), iOS ships via TestFlight, not this pipeline.

## Proven

- Real quake 2026-09-24 16:46 UTC: AWS chaparral HIT at +18.1 s; origin to push ≈ 18.8 s, of which ~0.78 s is ours.
- No Google account, x86_64 image, Brazil IP: works.
- Tests: gateway 55/55, JUnit, test_sensor_health, test_lab and test_verify, all green.

## In progress

- **Location freshness (QA-84/QA-90), settled design:** sensor-health reads AEA's own location age (GMS event log, remembered across log rolls), reboots at > 20 h, and on GpsKeeper receptors nudges 2 km out and back at > 6 h. Proven on quibdo 25-sep 05:30–05:40. quibdo has GpsKeeper; chaparral gets it with the next APK rollout, until then it relies on the 20 h reboot.
- **Certifier** (pid 30920 since 23:33 UTC): catalog-only ground truth (Mac retired), AEA age > 21 h = DEGRADED, live notice when a receptor is uncovered ≥ 15 min (DEGRADED) / ≥ 60 min (FAIL). On disk for the next restart: latency > 2 s rule, tunnel retry, lone-canary support (MONITOR_CANARIES).
- **devops (25-sep):** LIVE: CloudWatch (stacks `aea-lab-cloudwatch`, `aea-lab-alarms-i-0d1c0b6dd02e0ae61`; 5 alarms per host → SNS email: gateway down, receptor uncovered, location age > 21 h, AEA_NOT_OK x3 in 15 min, NUDGE_FAILED; ≈ $2/month) and HTTPS staging (stack `aea-lab-https`: EIP + CloudFront with secret origin header → Caddy allowlist; `infra/https/verify.sh` passes). Deploy role/bucket/instance profile from `aea-lab-deploy`. Not yet working: `deploy.yml` (needs the stack update), `deploy-apk.yml` (held by `APK_DEPLOY_ENABLED`). `aws-bootstrap.sh listener` now takes explicit `emulator-NNNN=id` pairs and merges the map.

## Open

- **Road to TestFlight (25-sep), assigned:** developer: /telemetry/arrivals (opt-in test phones) + monitor-keyed APNs burst + real-APNs readiness audit + 10k/100k scale benchmark. developer-ios: telemetry upload, PrivacyInfo, icon, accessibility audit, TestFlight config. developer-qa: pending reviews, then iPhone latency plan + certifier iPhone leg. devops: hourly gateway-state backups + recovery drill (RTO). investigator: Colombian legal (Ley 1581, Ley 1523, disclaimers, privacy policy draft). Blocked on the user: Apple Developer account, iPhone on iOS 26, Critical Alerts request, domain before launch.

- **MAX_DEVICES stays 10,000 through TestFlight (coordinator, 25-sep):** the only abuse guard on `/devices`. Raise it together with App Attest, before the public launch. Measured ceiling: ~100k iPhones per sensor for the 3 s fanout (load.md); past that, worker processes.

- **PWA (user, 25-sep):** testing only; final users are the native iPhone app. Take the PWA page off the public URL (CloudFront/Caddy allowlist) once the iOS app ships. Keep the Web Push backend: the certifier's end-to-end check uses it.

- **First pipeline deploy: DONE 25-sep (ff276ae, covered after 10 s, checksum verified).** Next, devops runs, one step at a time: swap.sh → probe install → host-alarms (7 alarms) → general-santos as emulator 3 (then STATUS deploy line EMULATOR_COUNT=3, certifier MONITOR_CANARIES=general-santos).
- **APK rollout** (pipeline, one receptor at a time, key switch to the CI key): chaparral gets GpsKeeper, quibdo the latest APK. Held until the first deploy works.
- **Canaries:** general-santos alone for now (4 emulators don't fit in 15.7 GB). glan, La Serena, San Salvador wait for dedicated hosts (~$33/month each) when there is money.
- **QA-67:** EC2 CPU during an event, measured at the next quake.
- **Native iOS app (AlarmKit):** later, after Xcode, the Apple Developer account and the developer-ios agent.
- **Custom domain:** optional and cosmetic. Staging HTTPS works at https://d1o3i68tksfjqi.cloudfront.net; the user buys a domain when ready.
- **Auto receptor growth:** built, propose-only (AUTO_RECEPTOR_BUDGET_USD=0) until the user sets a cap.
- **Extreme optimization (decision.md §9):** only when everything is stable.

Closed on 24/25-sep: Mac fleet retired (19:11 UTC); QA-90 (AEA location age + nudge, proven 25-sep 05:30–05:40 on quibdo); false uncovered on quibdo 21:15–23:25 fixed; outreach emails dropped.

Closed 25-sep (coordinator, ~23:20 UTC): all 4 queued QA-approved batches committed and pushed, explicit paths, tests re-run before each commit. `8087cce` gateway batch (test alert endpoint, arrival telemetry, APNs burst probe, real-APNs readiness, scale fixes; QA-107 cosmetic comment fixed inline), `8387d62` listener multi-pair install fix, `719b15c` hourly gateway-state backup + recovery drill, `f407540` iOS app (consent, coverage, telemetry upload, localization, TestFlight config). `8387d62` deployed and verified live. Follow-ups filed with developer-ios: QA-105 (medium, stale coverage after outage), QA-111/112 (low, actor-reentrancy races), QA-113 (test gap).

## Known traps (do not re-investigate)

- Shared working tree: all agents edit the same checkout. Never `git switch`/`checkout` a branch in it (it silently removes others' uncommitted edits). For a branch push use `git worktree add`.

- EMULATOR_COUNT=3 on the main host now: a bootstrap rerun with 2 would drop general-santos. Each emulator grows to ~3.5 GB RSS over hours; a 4th does not fit in 16 GB (QA steady-state numbers, 25-sep).
- CI's `ios-core` (macos-26) gates every gateway deploy. If GitHub macOS runners are down and a hotfix must ship, with the user's approval only: Actions → deploy → Run workflow on main with `ci_run_id` = the ci run of the push at the tip of main. It deploys only that tip, only if every job but ios-core is green, and still needs the `production` approval.
- AWS-RunShellScript runs with /bin/sh (dash): SSM scripts go through `bash -c "$(cat <<'X' ...)" </dev/null` (ssm-run.sh).

- zsh: `ubuntu@$IP:earthquakes-project` expands `$IP:e` as a modifier (extension). rsync/scp then copy to a LOCAL dir and the deploy silently lands stale code (24-sep 19:06). Always write `${IP}:`.

- The geo fix only lands with the GNSS HAL active. Without GpsKeeper, that is only during boot.
- GMS seems not to alert with a location older than ~24 h (QA-84).
- `cmd location set-location-enabled false` wipes the location. Do not use it.
- 2 vCPU cannot handle 2 emulators booting at the same time: boot them in sequence.
- targetSdk 35 blocks plain HTTP: cleartext allowed only to 10.0.2.2.

## Handoff per role (before a compact: each one leaves here where it stopped, 2-3 lines)

- **coordinator:** (25-sep ~23:20 UTC) Committed and pushed all 4 QA-approved batches queued from the handoff (see "Closed 25-sep" above); `8387d62` live and verified on host. Waiting on: devops-earthquake for the recovery-drill RTO result and confirmation the `aea-lab-drill` throwaway host + its EBS volume are gone; investigator's `docs/research/languages.md` (blocked on their WebSearch limit, resets ~21:30 local) to bring to the user; the user's OK on the backup batch's stack update + passphrase before devops runs install.sh. One stray "waiting" deploy run duplicate of the already-succeeded `8387d62` deploy sits in Actions — not approved, flagged to the user, harmless if left alone. Two loose ends from the prior coordinator handoff still open: delete the retired Mac AVDs (personal Google account), ntfy for certifier alerts — both unanswered by the user.
- **developer:** nothing half done. All green: test_sensor_health, test_receptor_placement, test_bootstrap_listener, JUnit + APK. QA-approved and waiting for the coordinator's deploy: demand-driven siting (`demand_cell` on /devices, `scripts/receptor-placement.py` + daily timer, budget default 0), canaries general-santos/glan (public:false), /subscribe public-only for users. Also QA-approved (same deploy): `sent_at_ms` on APNs alert pushes; coverage levels in `ios-contract.md` (app-side, matches `ReceptorChooser`); /devices takes `sensor_ids` + `demand_cell` together ("limited", verified by a background push) and `min_magnitude` per receptor (fanout skips weaker, null magnitude still sent); user text in usted (gateway, PWA, listener). In QA: QA-59 CI fix (coverage check sent stale news to a phone registering mid-check; test waits on evidence), "down" copy "Servicio interrumpido", `POST /devices/test` (503 with the kill switch on), placement ignores cells outside a Colombia box. Also in QA (25-sep): `POST /telemetry/arrivals` (test phones opted in by the monitor key, dedup on event_id+source, own file, `GET /telemetry`), `POST /probe/apns-burst` (T5, ≤50, ≥1 s), test push `event_id`, real-APNs readiness (startup refuses a bad key/ids, `APNS_PRIVATE_KEY_FILE`, ExpiredProviderToken renew+retry, DeviceTokenNotForTopic no longer deletes, `/status` `apns_dry_run`; env vars in gateway/README "Real APNs"). Scale (load.md): merged writes of devices.json, no throw on empty APNs bodies, 8 APNs connections; 100k store now POST p95 219 ms, loop stall 120 ms, fanout 2.8 s. Also in QA (high, rebuild.sh needs it): `aws-bootstrap.sh listener` with several pairs installed only the first and exited 0 (adb shell ate the loop's stdin). Fixed: aea_adb runs with </dev/null (in remote.sh too), loop reads fd 3, install count must match targets. Also in QA: APNs pushes carry `title-loc-key`/`loc-key`/`loc-args` (7 key pairs, table in ios-contract.md "Localized text"), Spanish title/body kept; gateway 87/87. APK (latency stamps, MY_PACKAGE_REPLACED) waits for its own deploy. Open follow-ups: `new_receptor` push and sensors.json reload without restart (not built). Traps: gradle needs `JAVA_HOME=/Library/Java/JavaVirtualMachines/jdk-17.jdk/Contents/Home`; `Map.groupBy` does not exist in Node 18; `node -e` breaks importing server.js, use a .mjs; script tests must never reach the real `lab.adb`.
- **developer-qa:** certifier pid 36901 (manual, `kill 36901`; restart = kill + `nohup python3 monitor/monitor.py run`, tell the coordinator first). Restarted 25-sep with general-santos: code defaults now cover the 3 AWS receptors (serials, canary general-santos, GpsKeeper on quibdo and general-santos), no env needed. On disk, loads at the next natural restart: the AWS receptor list comes from the host's sensor map instead of code (coordinator's request; dry run on the host read all 3), and an instance id that no longer resolves fails the location poll loudly. Reviewed infra/new-account: QA-94..97 fixed and approved 25-sep. After a move, repoint the certifier (MONITOR_INSTANCE, MONITOR_SSH_KEY, new-account creds). load.md "after" column filled 25-sep (3 emulators steady at 8 h). Push receiver restarted 20:45Z with the QA-101 keepalive fix (pid changes on respawn; the certifier respawns it). devops' ssm-run.sh stdin fix reviewed and approved 25-sep. Reviewed and OK'd 25-sep (queue order from the coordinator): developer's gateway batch (QA-59, /devices/test, telemetry, probe burst, real-APNs readiness, scale fixes — one server.js, approved as a unit; QA-106 localization risk refuted, QA-107 cosmetic nit), listener multi-pair fix (no findings), devops backup batch (design-review only, nothing deployed; QA-110 informational), iOS RelayCore (QA-105 confirmed with root cause + fix direction, QA-111/112 actor-reentrancy low findings, QA-113 test-coverage gap). Full detail: docs/qa/review.md. Open QA: QA-67, QA-90b, QA-93, QA-105, QA-107, QA-111, QA-112, QA-113. Tests: gateway 87/87, RelayCore swift test 115/115, test_bootstrap_listener 5/5, test_remote 14/14, test_backup_restore 4/4, test_rebuild 5/5, test_verify 26, test_sensor_health 16, placement PASS.
- **devops-earthquake:** (25-sep ~12:40Z) Pipeline live (ff276ae). general-santos live as emulator 3 since 12:00Z; ≥ 8 h measurement (MemAvailable, RSS, swap, alarms) due ~20:07Z → coordinator + developer-qa (load.md); 12:00Z baseline: MemAvailable 4641 MB, RSS 3480/3619/3360 MB, swap 0. `infra/new-account/` (backup.sh, envelope.py, rebuild.sh, README, tests) written, NOT pushed: QA-94..97 fixed, waiting for QA's mutation rerun; then the coordinator commits. Dedicated canary hosts (`infra/canary/`) deferred for cost. Test harness trap: never pipe a test to grep from a background task; the fakes must not read stdin unless the command takes input.
- **developer-ios:** (25-sep ~23:45Z) `ios-client/RelayCore` swift test 107/107 (incl. real-gateway contract test). App: consent screen before any prompt or POST (Ley 1581; `Consent` versioned, checked in TokenLifecycle.register, the one place a POST /devices starts), "no es un sistema oficial" line on consent and in Cobertura, PrivacyInfo.xcprivacy (app + NSE), purpose strings fixed (they said the location never leaves the phone; the ~11 km cell does), app icon (hand-drawn seismograph, `tools/app-icon.swift`; SF Symbols are not allowed in icons), es-CO only. Accessibility: `AccessibilityUITests` runs Apple's audit on every state at default and largest text size plus VoiceOver order, all green after fixes (button contrast, clipped alert text, Cobertura rows). TestFlight config: one `APP_BUNDLE_ID` in project.yml (app group derived, read from Info.plist), `DEVELOPMENT_TEAM` empty, aps-environment production in Release; the Release build was broken by DEBUG-only previews, fixed. User's list: `ios-client/TESTFLIGHT.md`. Device tests T1-T5: `ios-client/DEVICE-TESTS.md` (arrival log via `devicectl device copy from`). UI tests: TestAlertUITests needs a fresh local dry-run gateway on :8787; FirstRunUITests needs `simctl uninstall` first. Arrival telemetry upload built (ArrivalUploader: only when the 201 says telemetry, watermark, 500 per batch, 400/413 drop, 404 off until the next 201, 429/5xx Backoff; contract test opts the phone in with a monitor-signed call). App logs its own arrivals (source app, foreground). "Retirar consentimiento" in Cobertura: DELETE /devices, clears consent, set, fix and dead-man, back to consent; a failed DELETE is retried with backoff and the consent screen says so (checked on the sim with the local gateway up and down). PrivacyInfo now declares Performance Data. Traps: Swift 6.4 IRGen crash on typed-throws Task closures; simctl push does not run the NSE; an XCUI `buttons` query lists breadth-first, not in VoiceOver order.
- **comunicacion-humano** (session closed): outreach dropped by the user on 2026-09-24. Drafts stay in docs/outreach/ as history; nothing is sent.
